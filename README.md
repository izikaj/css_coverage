# Pupeter Coverage Extractor
Util to extract & save some pages used css.

# Install
Copy repo:
`git clone git@github.com:izikaj/css_coverage.git`

Install deps:
*install required chrome/chromium version!*
`npm install`

# Run
`npm start`
`cat dist/application.css | pbcopy`
